; Custom PHP settings for small VMs (e2-micro)
; Keep memory and execution time conservative to avoid server hangs.

memory_limit = 128M
max_execution_time = 30
max_input_time = 30
post_max_size = 32M
upload_max_filesize = 32M
display_errors = Off
log_errors = On
